<h1>Dashboard</h1>

<%= render "shouts/text_shout_form" %>
<%= render "shouts/photo_shout_form" %>

<!--in order to use @dashboard object you have to create one, so use Dashboard.new and so initialize-->
<%= render @dashboard.timeline %>
